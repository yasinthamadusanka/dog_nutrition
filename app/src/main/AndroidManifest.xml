<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DogNutrition"
        tools:targetApi="31">
        <activity
            android:name=".ArticleDetailActivity"
            android:exported="false"
            android:label="back to articles"
            android:theme="@style/ShowDetailsTheme" />
        <activity
            android:name=".ArticleActivity"
            android:exported="false" />

        <activity
            android:name=".SearchDetailActivity"
            android:exported="false"
            android:label="back to search"
            android:theme="@style/ShowDetailsTheme"/>
        <activity
            android:name=".SearchViewActivity"
            android:exported="false" />
        <activity
            android:name=".CartActivity"
            android:exported="false" />
        <activity
            android:name=".ShowDetailsActivity"
            android:exported="false"
            android:label="back to home"
            android:theme="@style/ShowDetailsTheme" />
        <activity
            android:name=".EditProfileActivity"
            android:exported="false"
            android:label="back to profile"
            android:theme="@style/ShowDetailsTheme2" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:label="back to home"
            android:theme="@style/ShowDetailsTheme2" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>